---
import type { CollectionEntry } from 'astro:content';
import { presets } from '../presets';

interface Props {
  demo: CollectionEntry<'demos'>['data'];
  href: string;
  onDark?: boolean;
  compact?: boolean;
}

const { demo, href, onDark = false, compact = false } = Astro.props;
const preset = presets[demo.style];
const classes = ['btn', `btn-${preset.buttonStyle}`, onDark ? 'btn-on-dark' : '', compact ? 'btn-compact' : '']
  .filter(Boolean)
  .join(' ');
---

<a href={href} class={classes}><slot /></a>
